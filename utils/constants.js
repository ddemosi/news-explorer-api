const resourceNotFoundMessage = 'Requested resource does not exist';
const authErrorMessage = 'Authorization required for this action';
const badRequestMessage = 'Invalid user or data';
const badEmailMessage = 'Invalid email format';
const validationErrorMessage = 'Email or password does not exist';

module.exports = {
  resourceNotFoundMessage,
  authErrorMessage,
  badRequestMessage,
  badEmailMessage,
  validationErrorMessage,
};
